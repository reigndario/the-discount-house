import { StubPage } from "@/components/StubPage";

export default function BookPage() {
  return (
    <StubPage
      name="Book"
      description="The Confidential Preference Book: where borrowers and lenders publish encrypted ranked preferences and discover the opposite side of the market through coarse public buckets, without revealing exact terms."
    />
  );
}
