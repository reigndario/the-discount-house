import { StubPage } from "@/components/StubPage";

export default function DocsPage() {
  return (
    <StubPage
      name="Docs"
      description="Documentation on how the protocol actually works: matching, bonds, vesting-backed collateral, and confidential settlement."
    />
  );
}
